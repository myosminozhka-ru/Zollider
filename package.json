{
  "name": "Zollider",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "devM": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt",
    "buildM": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "startM": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt start",
    "generateM": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt generate",
    "devW": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt",
    "buildW": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "startW": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt start",
    "generateW": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.6",
    "body-parser": "^1.20.2",
    "core-js": "^3.25.3",
    "express": "^4.18.2",
    "just-validate": "^4.2.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.3",
    "nuxt": "^2.15.8",
    "sass": "1.54.9",
    "sass-loader": "10",
    "swiper": "8.3.2",
    "vue": "^2.7.10",
    "vue-server-renderer": "^2.7.10",
    "vue-template-compiler": "^2.7.10"
  },
  "devDependencies": {
    "@nuxtjs/style-resources": "^1.2.1",
    "push-dir": "^0.4.1"
  }
}
